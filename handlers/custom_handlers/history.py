import sqlite3
import datetime




# def for_history(text, date):
#     connection = sqlite3.connect('my_database.db')
#     cursor = connection.cursor()
#     cursor.execute('INSERT INTO Commands (text, date_time) VALUES (?, ?)',
#                    (text, date))
#     connection.commit()
#
#
# for_history('fff', datetime.datetime.now())
